# External SDK consumer compile check

Stability: **testing**

This workspace package intentionally imports only the documented exports of
`@dstar/core`, `@dstar/node`, `@dstar/node/testing`, and
`@dstar/render-html`. Its successful build is
the Milestone 0 proof that a small external consumer can compile against the
published SDK entrypoints.
