# @dstar/render-html

Deterministic render-tree, canonical HTML, reviewable projection, mapping, and
safe package-asset utilities for DSTAR. The package renders snapshots but has
no canonical-content write path.
